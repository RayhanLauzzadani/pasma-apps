rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule - adjust based on your security needs
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
